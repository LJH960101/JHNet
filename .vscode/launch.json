{
    "version": "0.2.0",
    "configurations": [

        {
            "type": "lldb",
            "request": "launch",
            "name": "Lldb debug",
            "program": "${workspaceRoot}/src/main.out",
            "args": [],
            "cwd": "${workspaceRoot}"
        }
    ]
}